# Naming Services Integration

Contract IDs are unreadable. Use sorokeep watch with --name to show friendly names.

Resolve a name to a contract ID externally, then:

```bash
sorokeep watch <contract-id> --name my-contract
```
